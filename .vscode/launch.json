{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run EchoCode",
      "type": "extensionHost",
      "request": "launch",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}/extension", "${workspaceFolder}/demo"],
      "outFiles": ["${workspaceFolder}/extension/dist/**/*.js"],
      "preLaunchTask": "Build EchoCode extension"
    }
  ]
}
